name: Crytinox Backend Production CI/CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    name: Continuous Integration (CI)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code Repository
      uses: actions/checkout@v4

    - name: Set up Go Environment
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Verify Dependencies and Build Test
      run: |
        # No 'cd' needed. Files are at the root of the workspace.
        go mod download
        go build -v -o test_binary .

  deploy:
    name: Continuous Deployment (CD)
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
    - name: Deploy to AWS EC2 via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_SSH_KEY }}
        port: 22
        script: |
          # Server-side pathing stays as it is
          cd ~/CryptoProject-Backend
          git reset --hard origin/main
          git pull origin main
          go build -o crytinox_api .
          sudo systemctl restart crytinox